import React from "react";

const logo = (props) => (
  <div
    className={`md:mt-1 text-2xl text-[#af003c] tracking-[8px] ${props.classes}`}
  >
    <div>Mircale</div>
  </div>
);

export default logo;
